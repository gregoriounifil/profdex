VITE_API_URL=/api

# Origem do WebSocket do lobby de batalha (Socket.IO). Vazio = mesma origem
# (dev via proxy do Vite). Em produção com backend em outro domínio:
# VITE_WS_URL=https://profdex-production.up.railway.app
VITE_WS_URL=
